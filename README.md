# aeglobal

## Production environment

Safe production defaults are committed in `.env.production`.

Set this secret in Vercel Project Settings before deploying:

```bash
OPENAI_API_KEY=
```

Use `.env.example` as the template for local setup. Do not commit `.env.local`.
